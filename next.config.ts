import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  /*experimental: {
    // Force dynamic rendering for consistency between dev and prod
    workerThreads: false,
    // Enhance debugging information for RSC
    logging: { level: 'verbose' },
  },*/
};

export default nextConfig;
